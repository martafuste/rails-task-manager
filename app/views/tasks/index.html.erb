
<h1> Here all the tasks</h1>
<br>
<ul>
<% @tasks.each do|task|  %>
<li>
  <% if task.completed %>
    <i class="fas fa-check-square"></i>
  <% else %>
    <i class="far fa-square"></i>
  <% end %>
<%= link_to task.title, task_path(task.id) %> - <%= task.details %>
</li>
<% end %>
</ul>
